" Note-taking inspired by Notational Velocity.
Plug 'https://github.com/alok/notational-fzf-vim.git'

" Where to look for notes, first directory is the main directory where new
" notes are placed in.
let g:nv_search_paths = ['~/notes']
