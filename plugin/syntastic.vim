" Don't check syntax on openning and saving files
let g:syntastic_check_on_open = 0

" Don't check syntax on write
let g:syntastic_check_on_wq = 0

" Choose highlight to make the text visible, see syntastic help for details
" highlight SyntasticWarningLine ctermfg=white

" Syntax checker for javascript (default is jslint and jshint)
" let g:syntastic_javascript_checkers = ["jshint"]

" Syntax checker for typescript (default is empty)
" let g:syntastic_typescript_checkers = ['tslint', 'tsc']
