" Use the jedi autocompletion library for vim.
" Source: https://github.com/davidhalter/jedi-vim

" jedi-vim does not have 'loaded' config, have to use this as a workaround
if !exists('*Pyimport')
    finish
endif

