## Get the visual selection
Check out `openbrowser` to see how the `<Plug>` key map is done.
OR check out `dispatch` command `0Start`

## Go to definitions in included files
Configure included files: `include` and `includeexpr` options.

Go to definitions in included files: `ijump` and the likes.

